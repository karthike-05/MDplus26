# Catalyst-26 backend deps. Floors are set to known-good majors; after your first
# clean install run `pip freeze > requirements.lock` and commit that so all four of
# us build against identical versions (see CLAUDE.md §10 — drift is a parallel-build hazard).

# --- Web / app ---
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6

# --- DB (the single vendor-facing layer: backend/db/supabase.py) ---
asyncpg>=0.29

# --- Agent / LLM ---
anthropic>=0.40

# --- Form automation ---
playwright>=1.44          # web injector; run `playwright install chromium` once
pymupdf>=1.24             # fitz — pdf overlay + page rendering
pypdf>=4.2                # AcroForm fillable pdfs

# --- Messaging / voice ---
twilio>=9.0
# retell-sdk              # add when Pranav/Aneesh wire voice+SMS

# --- Utils ---
python-dotenv>=1.0
httpx>=0.27

# --- Tests ---
pytest>=8.2
pytest-asyncio>=0.23      # L1 async tests (the db seam is async now)
